import 'package:flutter/material.dart';
import 'playerWidget.dart';
import 'nowPlaying.dart';
/*
class PlayerPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    var nowPLayingWidget = NowPlayingWidget(song: fetchNowPlaying());

    return new Scaffold(
      appBar: new AppBar(title: new Text("Bide & Musique")),
      body: Column(
        children: [
          Expanded(
            child: nowPLayingWidget,
          ),
          Expanded(
            child: PlayerWidget(),
          ),
        ],
      )
    );
  }
}
*/
